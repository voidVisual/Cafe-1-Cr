{
  "name": "cafe-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "services/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "docker-compose up -d && npm run start:all",
    "start": "npm run start:prod -w cafe-backend",
    "start:all": "concurrently \"npm run dev -w frontend\" \"npm run dev -w admin-dashboard\" \"npm run start:dev -w cafe-backend\"",
    "build": "npm run build -w cafe-backend && npm run build -w admin-dashboard && npm run build -w frontend"
  },
  "dependencies": {
    "caniuse-lite": "^1.0.30001805"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
